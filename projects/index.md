# 项目状态表

Agent 启动时读本表，只加载 `active` 且与当前任务相关的项目。禁止全量读取 projects/。

| 项目 | 状态 | 一句话描述 | 最近更新 |
|---|---|---|---|
| _example | active | 示例项目，建新项目时参照其三件套结构 | 2026-08-19 |

状态取值：`active`（进行中）/ `paused`（暂停）/ `done`（完结，仅存档）
